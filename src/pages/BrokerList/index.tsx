import styled from "styled-components";

const BrokerList = styled.div`
  color: rgb(var(--dark-color), 0.5);
`;

const index = () => {
  return <BrokerList>index</BrokerList>;
};

export default index;
